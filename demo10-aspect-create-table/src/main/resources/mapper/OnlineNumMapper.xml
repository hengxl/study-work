<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hxl.aspect.mapper.OnlineNumMapper">

     <select id="selectOnlineNum" resultType="java.lang.Integer">
        select ifnull(sum(t.total_online_num), 0)
        from (
            select total_online_num
            from ${tableName}
            where substring(end_time, 1, 10) &gt;= #{startTime}
            and substring(end_time, 1, 10) &lt;= #{endTime}
        ) t
    </select>

</mapper>