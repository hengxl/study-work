<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hxl.aspect.mapper.UserMapper">

    <select id="queryUser" resultType="com.hxl.aspect.entity.User">
        select * from ${tableName}
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="userName != null">
                and user_name = #{userName}
            </if>
        </where>
    </select>

     <select id="queryUserDto" resultType="com.hxl.aspect.entity.UserDTO">
        select * from ${tableName}
         <where>
             <if test="userId != null">
                 where user_id = #{userDto.userId}
             </if>
         </where>

    </select>

</mapper>